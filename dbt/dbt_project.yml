name: cricket_analytics
version: '1.0.0'
config-version: 2

profile: cricket_analytics

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cricket_analytics:
    silver:
      +schema: silver
      +materialized: table
    gold_ml:
      +schema: gold_ml
      +materialized: table
    gold_ba:
      +schema: gold_ba
      +materialized: table
